# AI Handoff

Status: ready
Owner: none
Branch: `main`
Last-known commit: `b3ff01d`

## Latest result

- 2026-07-04: Updated the fundraising figure to the FINAL 2025–2026 total of **$30,104** and removed the $50,000 goal / percentage framing everywhere (year is complete). (1) `FundraisingTracker.jsx` now dual-mode: when `goal` is 0/falsy it shows a full celebratory ring with the dollar total in the center + a `periodLabel` ("2025–2026") instead of "% of goal"; the goal-mode branch (default `goal=50000`, "of goal", "$X / $50,000") is kept intact for a future year with a goal. (2) `Philanthropy.jsx`: hardcoded `raised = 30104`, `YEAR_LABEL = '2025–2026'`; tracker now `<FundraisingTracker raised={raised} goal={0} periodLabel={YEAR_LABEL} />`; marquee reads "$30,104 raised in 2025–2026"; section copy changed from "Live totals … updated three times daily" to "Our 2025–2026 total …". (3) `StJudeSection.jsx` (LIVE homepage badge): "$50K / Annual Goal" → "$30,104 / Raised in 2025–26". (4) `ImpactStats.jsx` (legacy /home-classic only): raised→30104, dropped the "Goal Progress %" tile (removed unused TrendingUp import), grid now 3-col. DOM-verified all four spots + zero leftovers of $50,000/$50K/Annual Goal/of goal/$12,400 site-wide (screenshots unreliable here — paused-rAF/Lenis quirk). Checks: `npm run lint` clean, 38/38 tests, build exit 0.
- 2026-07-04: Hid the Exotics Car Show ("Friday Night Lights", Aug 28) registration page per user request, reversibly. Commented out (not deleted): the `ExoticsCarShow` lazy import + its two routes in `src/App.jsx` (`/exotics-car-show` now falls to the 404 page; `/events/exotics-car-show-2026` falls to the generic `/events/:slug` → EventSignup's on-brand "Event not found" card — verified both), the `exotics-car-show-2026` entry in `src/lib/news.js` (the "Apply to Display" news CTA), and the whole "Friday Night Lights" promo card in `src/pages/Philanthropy.jsx` (+ its now-unused `EXOTICS_SHOW` import). Also re-pointed the "Official Car Show Sponsorship" card's "View paid sponsorship details" button (was `Link to="/exotics-car-show"`, a pre-existing mislink) to the `mailto:slutkestewardship@gmail.com?subject=TKE%20Car%20Show%20Partnership` sponsor-inquiry channel so it doesn't dead-end. AdminRegistrations.jsx's internal slug check was left alone (staff tool, not a public entry point). All hidden blocks are clearly marked "HIDDEN — restore ... together" for easy revert. Checks: eslint/`npm run lint` clean, 38/38 tests, build exit 0. Browser-verified both hidden routes + Philanthropy page (no dead links, no layout gap).
- 2026-07-04: Reverted the intro "TKE" wordmark to the ORIGINAL plain Playfair text (`font-heading font-bold`, `layout.base*0.2`, inline `motion.span` at `translate(-50%, -50% - base*0.09)`, zIndex 40) per user request — removed the SVG letterform experiments (the collegiate slab-serif and the fishtail traces, `TKE_PATHS`/`TkeWordmark`, both now gone). The triangle photo alignment + flipped connect-triad from earlier today are KEPT. NOTE: user tried two logo-accurate SVG traces and preferred the plain original; don't re-add SVG letterforms without asking. Checks: eslint clean, 34/34 tests, build exit 0; screenshot-verified triangle beat at 1280×800 + 375×812.
- 2026-07-04: LivingBond intro polish (`src/components/next/LivingBond.jsx` only). (1) "TKE" wordmark is now hand-traced SVG collegiate letterforms echoing the official mark (flared serifs, hanging T-bar serifs, white + dark keyline, skewX(-7), zIndex 40 above photos) — no webfont added; `TKE_PATHS` on a 100-unit grid. (2) Triangle beat: photos distribute evenly per edge (5/5/4 desktop), padded 7.5% from vertices, rotated ±60° to lie along the slanted edges; parallax damped to the thread layer's 10px during triangle/principles so photos stay on-edge; stronger white frame ring; whole triangle lifted 0.06·base for page centering. (3) Connect-beat triad flipped to inverted orientation (two up, one down) + spacing relaxation now anchors indices <3 (they push, never move) so the triad keeps its spread. (4) Dev-only `?bond=<phase>` query pin (guarded by `import.meta.env.DEV`): initializes phase state directly + `initial={false}` via `dbg` prop so a beat renders pre-settled even in a hidden tab (rAF paused) — this is how to visually verify beats in the preview.
- Checks: eslint clean, 34/34 tests, build exit 0. Screenshot-verified at 1280×800 and 375×812 (triangle, principles, connect beats). Natural timeline logic untouched.
- 2026-06-24: New homepage entrance "THE LIVING BOND" (`src/components/next/LivingBond.jsx` + data in `src/lib/chapterMemories.js`); `HomeNext.jsx` renders it instead of `BrotherhoodIntro` (kept as alt) + a discreet "Replay" control. Concept: one cherry-red heartbeat → threads connect real chapter photos into a living constellation → journey through generations (LOVE·CHARITY·ESTEEM) → photos assemble into the INVERTED TKE triangle (apex down) with a white "TKE" wordmark filling it like the logo ("Bound for life") → contract into a single-line Gateway Arch → red portal → existing ForgeHero. Tech: CSS 3D transforms + SVG threads + framer-motion (NO WebGL, no new deps; transform/opacity only). Rendered via `createPortal(document.body)` because Layout's page-transition `motion.div` transform was the containing block for `fixed` (overlay measured 6753px tall before the portal fix). ~11.5s, skippable (button/Escape/scroll), sessionStorage `tke-livingbond-seen` (full once/session, ~1.3s short reveal after), reduced-motion + repeat → calm short reveal, scroll locked then restored, pointer parallax desktop-only (no device-tilt), responsive photo counts (14/11/7), spacing-relaxation so cards don't clump. Photos use the 512px `/thumb/` variants. NOTE for future debugging: the preview tab runs `document.hidden=true` (framer rAF paused) — to inspect a beat, paint the OUTER wrappers (children of `.left-1/2.top-1/2` stage), NOT `img.closest('.absolute')` (the img itself has class `absolute`, so that selector hits the img and translates it out of its `overflow:hidden` card → looks black).
- Checks: lint clean, 33/33 tests, build passes. Browser-verified (1280×800, manual-paint per beat): constellation (14 real photos spread w/ red threads) and triangle (photos form the TKE triangle) both look premium; full run completes → ForgeHero with scroll restored + no horizontal overflow; repeat visit = short reveal; mobile 375px no overflow; only Base44 404 console noise.
- 2026-06-24: Performance + QoL audit. (1) Route-level code splitting in `App.jsx` (React.lazy + Suspense; inner Suspense in `Layout.jsx` keeps nav during page loads) → entry bundle 1.6MB→582KB; `three` (489KB) and `maplibre` (1MB) are now on-demand chunks. (2) Removed a duplicate Lenis instance in `HomeNext.jsx` (Layout's `useLenis` already provides smooth scroll — two RAF loops were fighting). (3) `AuthContext.jsx`: 6s timeout on the Base44 public-settings request so a slow/dead backend can't block render; its handled failure is now `console.warn` not `console.error`. (4) Image optimization with `sips` (no new dep): `public/assets/photos/*.jpg` 14MB→6.7MB (resized to 1280px/q75, EXIF orientation preserved) + new `/photos/thumb/` 512px set (1.4MB) used by the intro → homepage image payload ~14MB→~1.3MB. (5) Added `loading="lazy"`/`decoding="async"` to below-the-fold imgs that lacked them (footer, instagram, event card, sponsor rail, member avatars, St. Jude); grids were already lazy. (6) Removed 6 unused deps (lodash, jspdf, html2canvas, @hello-pangea/dnd, react-quill, react-leaflet) via `npm uninstall`. (7) Vite `manualChunks` isolates three/recharts. (8) `index.html`: absolute OG image, canonical, twitter image/title/description.
- Checks: `lint` clean, `test` 33/33 pass, production `build` exit 0. Typecheck has only PRE-EXISTING errors in untouched `ResetPassword.jsx`/`Terms.jsx` (loose JSX typing). Browser-verified: home renders with thumbnails + single Lenis, code-split route navigation works (nav persists), no new console errors (only the legacy Base44 SDK 404 noise, unchanged). Visual design/branding/animations unchanged.
- 2026-06-23: Home intro is now a ~15s THREE-ACT cinematic in `src/components/next/BrotherhoodIntro.jsx` (`HomeNext.jsx` imports it instead of `SatelliteIntro`, kept as fallback). ACT 1 COSMOS: real satellite Earth (keyless MapLibre globe + Esri imagery, reused from SatelliteIntro) rotates in space → camera flies down to a top-down continental US; HUD label EARTH→NORTH AMERICA→UNITED STATES + ticking coords. ACT 1½ ORBIT→FILL: the `OrbitFill` component (one rAF loop, 2D screen space) flies 20 brother photos around Earth (front bigger, back faded so they pass behind the globe), then — staggered by the descent progress `pv` — peels them out of orbit one at a time to fill a 5×4 grid covering the page; on implode they vacuum to center. Replaced the earlier CSS-3D ring + bloom-grid. ACT 2 NATION: wall holds over the satellite US with a glowing chapter network (gold arcs + travelling light pulses + pins) radiating from a St. Louis "EPSILON ALPHA" hub; word-by-word headline "From every corner of the country." Verified VISUALLY via painted-frame screenshots (orbit around the real Earth globe, sequential fill, full "NORTH AMERICA" wall). Note: the live preview tab reports document.hidden=true intermittently (pauses rAF) AND collapses the viewport to ~1px on reload — use preview_resize to an explicit 1280×800 after each reload when inspecting; a 9.5s safety timer advances past cosmos if rAF stalls. Focused tabs run ~14s total. ACT 3 SEAL: nation implodes into center → gold seal materializes (spin rings, glow, line-drawn rings, ΤΚΕ monogram + curved text) → STAMP (shockwave + starburst rays + spark burst) → crimson bleed → white flash → ForgeHero. Plus embers, film grain, letterbox throughout. Filler tiles cycle the 23 `/public/assets/photos/*.jpg`; swap `PHOTOS` for headshots later. Once/session (`tke-intro-seen`), Skip, reduced-motion + maplibre-failure fall through.
- BUG FIXED during build: the downstream phase-timer effect was keyed on `[phase]`, so the first transition (implode) tore down the still-pending sigil/stamp/flash timers and froze the intro. Now scheduled ONCE when Act 2 begins (via `downstream`/`dsTimers` refs), cleared only on unmount.
- Checks: production build passes (exit 0), eslint clean. DOM-sampling in dev confirmed full order cosmos→nation→sigil→stamp→flash→gone with maplibre canvas, 48 tiles, chapter-network SVG, seal text, stamp ring, flash all present; only expected Base44 404 console noise. NOTE: preview tab is `document.hidden` so setTimeout is throttled — DOM timings look stretched (~19s); a focused tab runs the intended ~15s.
- NOT committed/pushed yet — awaiting user go-ahead.

## Next action

On user approval: commit (Anthony-authored) + push origin/main + verify live. The news.js car-show pricing fix ($30 / $50-after-Jul-15, drop rain date) is still outstanding.
